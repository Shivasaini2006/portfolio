# Copy this file to .env (not committed) and fill secrets
# Admin / notification email configuration
ADMIN_EMAIL=shivasaini1938@gmail.com
CONTACT_TO_EMAIL=shivasaini1938@gmail.com
# From email must be a verified sender in Resend (adjust domain or use onresend.com sandbox)
CONTACT_FROM_EMAIL=Portfolio Contact <no-reply@yourdomain.com>

# Authentication secrets (choose stable random values)
AUTH_SECRET=replace_with_long_random_string
JWT_SECRET=replace_with_long_random_string
VITE_API_BASE=http://localhost:3000  # vercel dev default; leave empty when deploying

# Resend API key (keep private, do not commit real key)
RESEND_API_KEY=your_resend_api_key_here

# Additional optional configuration
# MONGODB_URI=your_mongodb_connection_string
